<div style="text-align: center; padding: 60px; background: linear-gradient(120deg, #ff9ecd, #b28dff, #8ecbff); animation: fadebg 6s infinite;">
    <h1 style="font-size: 70px; color: #fff; margin: 0; font-family: 'Comic Sans MS', cursive; text-shadow: 3px 3px 10px rgba(0,0,0,0.3); animation: float 3s ease-in-out infinite;">
        🎉 CI/CD pipeline works 🎉
    </h1>
</div>

<style>
    @keyframes fadebg {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-12px); }
    }

    @keyframes pop {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }
</style>
